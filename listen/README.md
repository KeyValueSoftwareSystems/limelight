# listen — song → map

**Owner: Amal.** Turn a real song into a real map file, so nobody works against invented data.

## Tonight (2 hours)
Real beat times from a real song, **on your laptop, CPU only**. Ten lines with librosa is fine.
Do not start on the GPU box tonight — let that run as background chores.

**Done:** a file of real numbers from a real song, committed.

## This week
Stems, chapters, key, then the six moments. Write it in the shape in `../MAP.md`.
Then run it on Opus and Strobe and **write down exactly how it fails** — that is the discovery,
not the failure.

## Do not start with training
Training is the fun part and it is not the job yet. Week one is "make maps and find out how bad
they are." You cannot improve what you cannot measure.

## Week two
`vectors` — one set of numbers per beat, from an existing open music model. **Inference, not
training.** An afternoon's work, and it is what makes this an AI file rather than a spreadsheet.
