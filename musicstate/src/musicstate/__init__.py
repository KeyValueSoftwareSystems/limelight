"""musicstate — turn an audio file into a layered MusicState, then a MAP v0.3 file.

Kept import-light on purpose: importing the package pulls in no heavy audio deps.
Import what you need explicitly, e.g. ``from musicstate.pipeline import build`` or
``from musicstate.port import to_map``.
"""

__version__ = "0.2.0"
